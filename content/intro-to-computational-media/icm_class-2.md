---
date: 2025-09-10
tags:
  - lectures
noteOrder: "46"
draft: "true"
---
discussed portraits ([[intro-to-computational-media/self-portrait|self-portrait]]), spoke about variables, and solved coding-problems. 
# notes: 
- `push` and `pop` push into and pop out of a new matrix. 
- `pgraphics` gives a transparent background (to draw on top of things; perhaps like a ui). 
- `let` creates (and assigns) a piece of memory. each variable stores a piece of memory. i'm curious about memory — does that mean that when i load a p5 sketch, it is slower than running something else? can i, perhaps, optimise that in some way?  what is the pure way to make graphics on the screen? 
- in an error, `console.log` says line number: character number on which an error occured. 
- c++ has memory management options (don't store this variable after this). javascript does not, but you can do things to save memory — such as inputting variables from function to function. 

---

realised i can return multiple values through a function, and access only singular ones of them to reduce memory usage (by not storing global variables). 

![[z_images/Screenshot 2025-09-10 at 13\.56\.38.webp]]